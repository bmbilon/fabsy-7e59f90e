import { RAPID_RESOLUTION, RAPID_RESOLUTION_BUNDLE } from './offers';

export const PRO_DRIVER_DISCOUNT_PERCENT = 20;
export const PRO_DRIVER_COUPON = 'PRO20';
export const PRO_DRIVER_RAPID_CENTS = Math.round(RAPID_RESOLUTION.priceCents * 0.8);
export const PRO_DRIVER_BUNDLE_CENTS = Math.round(RAPID_RESOLUTION_BUNDLE.priceCents * 0.8);
