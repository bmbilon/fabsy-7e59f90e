#!/usr/bin/env node

console.error('This legacy AI city-page generator is disabled because it bypasses the reviewed-page guardrails.');
console.error('Create reviewed ssg-pages JSON and run scripts/seed-reviewed-pages.mjs instead.');
process.exit(1);
