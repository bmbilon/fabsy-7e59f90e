#!/usr/bin/env node

console.error("This legacy generator is disabled because it predates Fabsy's current content guardrails.");
console.error("Create reviewed ssg-pages JSON and run scripts/seed-reviewed-pages.mjs instead.");
process.exit(1);
